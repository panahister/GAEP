<!-- Copyright (c) 2026 Mohammad Maheri. Licensed under Apache 2.0. See LICENSE. Attribution required - see NOTICE. -->
---
generatedBy: AI-GCE
generatedVersion: "{version}"
source: "ai-gce-rules/core-engine.md"
generatedOn: "{generation-date}"
ownership: generated
---
# Hook Enforcement Guide — Template

Generated into `.governance/hooks/ENFORCEMENT-GUIDE.md` in the target workspace. This template is populated with project-specific hooks during AI-GCE generation.

---

```markdown
# Hook Enforcement Guide — {project_name}

Generated by AI-GCE | {date}

## How Hooks Work

Hooks fire automatically on IDE events. They warn when compliance rules are violated. Silence = everything is fine.

- **🔴 Essential:** Security-critical. NEVER remove.
- **🟠 High-value:** Architecture/governance enforcement. Remove last.
- **🟡 Advisory:** Quality suggestions. Remove first if overwhelmed.

---

## Currently Installed (Tier {current_tier})

{List of currently installed hooks with noise classification}

---

## Tier 1 — Day 0 (Structure + Safety)

| Hook | Event | Noise | What It Does |
|------|-------|:-----:|-------------|
| session-discipline | promptSubmit | 🟡 | Spec-before-code, never-vibe-code |
| pre-code-spec-check | preToolUse | 🟠 | Warns if no spec before implementation |
| api-contract-check | fileCreated | 🟠 | Contract must exist before controller |
| security-gate-check | fileEdited | 🔴 | Auth on all endpoints |
| migration-safety | fileEdited | 🔴 | Rollback method required |
| sensitive-data-check | fileEdited | 🔴 | No secrets/PII in code |

> **Process agents (invoke via shortcut — no hook needed):**
> - `CAA__` — Full compliance audit (was `periodic-audit.json`)
> - `PRC__` — Pre-PR quality checklist (was `pre-pr-checklist.json`)

## Tier 2 — Sprint 2+ (Governance + Quality)

| Hook | Event | Noise | What It Does |
|------|-------|:-----:|-------------|
| post-task-governance | postTask | 🟠 | Architecture + DoD after task |
| segregation-check | postTask | 🟠 | Author ≠ reviewer |
| **session-end-compliance** | **agentStop** | **🟠** | **Consolidated sweep: module boundaries + domain purity + coverage + naming (one pass, one report)** |

> **Consolidation (Lesson):** Individual `agentStop` hooks (module-boundary-check, domain-layer-purity, coverage-check, naming-check) are replaced by a single `session-end-compliance.json` hook that runs all checks in one pass. This reduces hook noise from 4-6 separate firings to 1, keeps context usage minimal (one prompt vs. six), and produces a single consolidated report. The individual JSON files are retained as reference (they document the rule logic) but are NOT installed as hooks.

## Optional — Family-Wide (opt-in, ships DISABLED)

| Hook | Event | Noise | What It Does |
|------|-------|:-----:|-------------|
| package-activation-guard | promptSubmit | 🟡 | Re-checks the AI-* multi-package switching rule on every prompt: no package switch without an explicit `_{PKG}_` key or confirmation; announces the active package on switch; `_ACTIVE_` reports the current one. **Not installed by default** — set `"enabled": true` or toggle in the Kiro Hooks UI. Useful only when several AI-* packages share one workspace. |

> **Note:** `steering-quality-check` and `documentation-reminder` are now included in the session-end-compliance sweep (checks #5 and #6, Tier 3 only). They no longer need separate hooks.

## Tier 3 — Pre-Release (Change Management)

| Hook | Event | Noise | What It Does |
|------|-------|:-----:|-------------|
| change-readiness-gate | preTask | 🟠 | CM artifacts before Integration tasks |
| exception-expiry-check | userTriggered | 🟠 | Expired rule bypasses |

> **Note:** `pre-pr-checklist` was converted to a process agent (`PRC__`). Invoke it via shortcut before creating a PR.

## Conditional (Only If Applicable)

| Hook | Condition | Event | What It Does |
|------|-----------|-------|-------------|
| tenant-isolation-check | multi-tenancy.md | fileEdited 🔴 | Tenant entity check |
| {others derived from conditional steering} | | | |

---

## Reducing Noise

If hooks produce too many warnings, remove in this order:
1. `session-discipline.json` (fires every prompt — highest noise)
2. `session-end-compliance.json` (advisory batch — one hook, all non-critical checks)

**NEVER remove:** security-gate-check, migration-safety, sensitive-data-check, tenant-isolation-check

---

## Adding Hooks

To activate next tier: say "Activate next compliance tier" — AI-GCE handles it.
Or manually: copy the hook .json files listed above into `.governance/hooks/`.
```
